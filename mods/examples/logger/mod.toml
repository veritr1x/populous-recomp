# The before-hook example: counts turns and writes what it counted.
id = "example.logger"
name = "Logger example"
version = "1.0.0"
api = 1
game = "815ba8a550f571c3"
requires = ["core.hooks >= 1.0.0"]
affects_simulation = false

[plugin]
path = "logger.dylib"
